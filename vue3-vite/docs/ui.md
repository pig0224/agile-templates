# UI 设计约定（项目级）

> 本文件是项目 UI 设计 token 与使用规则的**单一事实源索引**。上层规范（团队/产品级）优先级见 `CLAUDE.md`；本文件未覆盖的场景遵循上层规范。

## 设计 token 清单

<!-- 新增/修改 token 必须同步本表（与代码中的 token 定义保持一致）；原型与页面实现引用 token 名，不写裸色值 -->

| Token | 值 | 用途 | 备注 |
|---|---|---|---|
| `--color-primary` | 待定 | 主色（主操作、链接） | 待 UI 约定确认 |
| `--font-size-base` | 待定 | 正文字号基准 | 待 UI 约定确认 |
| `--radius-base` | 待定 | 基础圆角 | 待 UI 约定确认 |
| `--spacing-unit` | 待定 | 间距基准（如 4px 网格） | 待 UI 约定确认 |

## Token 管理方式

<!-- 待 /agile:init 约定问答确认后改写：CSS 变量 / JS token 文件 / 组件库主题配置 -->

- 管理方式：待定（模板默认：CSS 变量）
- 定义位置：待定

## 使用规则

1. 页面实现与原型引用 token 名，不硬编码色值/字号/间距。
2. 新增 token：先在本表登记，再落代码；修改 token 同步更新本表。
3. 暗色模式：待定（是否需要经 `/agile:init` 约定问答确认）。

## 上层规范引用

- 产品级 UI/交互规范：`biz-product-docs/`（抽屉三）。
- 团队框架领域：`biz-tech-docs/frameworks/<前端栈>/`（团队库匹配确认后生效，见项目 `CLAUDE.md`）。
