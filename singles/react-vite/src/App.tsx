export default function App() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: 32 }}>
      <h1>{{name}}</h1>
      <p>由 agile init project &lt;name&gt; --template react-vite 生成</p>
    </main>
  );
}
