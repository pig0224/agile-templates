export default function App() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: 32 }}>
      <h1>{{name}}</h1>
      <p>由 agile init project --type react 生成</p>
    </main>
  );
}
